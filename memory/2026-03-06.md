# Memory - Friday, March 6, 2026

## 11:48 PM - Configuration Lockdown: `OPENAI_API_KEY`
- **Issue:** The `OPENAI_API_KEY` persisted despite deletion attempts, causing unexpected fallback behavior to `gpt-5.2` (which failed) when `gemini-3-pro-preview` was unavailable.
- **Root Cause:** The environment variable is injected by the host container (VPS/Docker) on startup, making it immutable from within the container's standard configuration files.
- **Resolution:** Added `OPENAI_API_KEY=""` to `~/.openclaw/env` to forcibly override the inherited value with an empty string. This ensures the key is effectively nullified upon environment load.
- **Current State:** Verified `OPENAI_API_KEY` is empty in the current shell session.
- **User Feedback:** Expressed strong frustration ("tbk!!!") regarding the persistence of old configurations. Acknowledged the need for a definitive fix.
