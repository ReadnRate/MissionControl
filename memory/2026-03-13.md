
## 2026-03-13 23:33 EST: System Outage & Mission Control Migration
- **Critical Failure Incident:** OpenClaw gateway entered a crash-loop due to invalid syntax in `openclaw.json` (Vertex AI `models` was a string instead of an array). System was down for ~24h. The user was severely impacted.
- **Mission Control Outage:** After restart, Mission Control V4 (SPA) failed to auto-start because PM2 services (`mission-backend` and `mission-tunnel`) did not survive the cold boot automatically. Attempted manual recovery, serving the raw V4 dist.
- **Next.js Migration Confirmed:** The user explicitly forbade rolling back to V4. They ordered us to push forward with the `mission-control-next` migration (TASK-019).
- **PM2 Reconfiguration:** Deleted the legacy `mission-backend` from PM2. Started the new Next.js app (`npm run dev -p 8080`) under PM2 as `mission-next`, preserving the `mission-tunnel` (Cloudflared).
- **Supabase Data Loss Issue:** The Next.js app is showing errors because the Supabase database (`zexumnlvkrjryvzrlavp`) schema is entirely missing/dropped (except for one table). The user refused to manually run the `schema.sql` via the web dashboard, asserting it is Joe's responsibility as the orchestrator to execute DDL/SQL commands programmatically to fix the database.
- **Learnings Registered:**
  - LRN-20260313-001: Do not ask for known infra info (Vercel vs Local).
  - LRN-20260313-002: Do not hallucinate infra details based on general user preferences.
  - LRN-20260313-003: Always check/resurrect PM2 before attempting manual script launches.
  - LRN-20260313-004: Delete legacy PM2 services immediately when migrating to prevent zombie rollbacks.
  - LRN-20260313-005: Never suggest rolling back to legacy code if a migration has started. Always "fail-forward".
