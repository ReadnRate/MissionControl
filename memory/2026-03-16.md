
## 2026-03-16 Session Summary (Evening Flush)
- **Token Management & Context Window**: Learned a critical lesson about protecting the context window. Ran unbounded `find` commands and dumped 4 full documentation pages via `web_fetch` directly into the chat, causing severe token bloat (115k+). Created a new CRITICAL rule in `SOUL.md` to never dump large raw text and always limit terminal outputs (`| head -n 20` or dump to temp files).
- **Tavily MCP Activation**: Restarted OpenClaw Gateway to properly activate the Tavily MCP server (`mcp-tavily.json`) as dictated by `SKILL_PROCEDURES.md`, eliminating the need for bulky `web_fetch` operations for search and documentation.
- **Lead Gen Architecture Pivot (TASK-020)**: Stopped using Firecrawl for initial discovery to save credits. Designed a new workflow: 
  1. Free URL generation via `duckduckgo-search` (cli: `ddgs`). Wrote `free-url-finder.sh` to scrape URLs and push to an n8n webhook.
  2. n8n workflow handles extraction: Webhook -> Tavily Crawl (for deep text extraction) -> OpenAI (for structured JSON parsing) -> Supabase.
- **n8n API Limitation Learned**: Attempted to use `n8n_api.py create` to generate the new workflow programmatically. This resulted in incomplete, broken workflows because the n8n API struggles with modern/LangChain nodes. Reaffirmed the rule in `SKILL_PROCEDURES.md` to **NEVER** create workflows via API and rely strictly on manual UI creation by the user for new flows.

### Late Evening Update: n8n Workflow Creation & Version Compatibility
- **Policy Update (`SKILL_PROCEDURES.md`)**: Modified the strict rule against creating n8n workflows via API. Added an exception to allow it *when explicitly requested by the user*. This resolves a conflict where rigid adherence to procedures prevented me from fixing a broken workflow for Manuel.
- **n8n Self-Hosted Constraints (v2.11.4)**: Diagnosed a UI corruption error ("could not find property") when importing n8n workflows. The self-hosted version does not support advanced LangChain/OpenAI specific nodes (`@n8n/n8n-nodes-langchain.openAi`). 
- **Workflow Resolution**: Rebuilt and deployed the "Author Lead Generation" workflow using only universally supported basic nodes (`n8n-nodes-base.httpRequest` for both the Tavily crawl and the OpenAI API completions). The stable, compatible workflow ID is `iALYZtyN2jNK6McE`.

### Late Evening Update (Continued): n8n Architecture Correction
- **Correction**: Manuel rightly called out that using basic `httpRequest` nodes instead of proper AI Agent nodes makes the workflow brittle and non-performant. He also reminded me that we should leverage MCP tools natively within n8n.
- **Architectural Shift**: We are abandoning the linear HTTP-based n8n workflow. The correct architecture is an **AI Agent Node** equipped with a Chat Model and a Tool (Tavily native tool or MCP server).
- **Model Preference**: Manuel explicitly requested to use **Gemini** as the primary model in n8n, rather than OpenAI `gpt-4o-mini`. This aligns with our recent transition to Vertex AI / Gemini across the ecosystem.
