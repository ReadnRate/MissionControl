# BOOTSTRAP.md
OpenClaw boot complete. All core files (AGENTS, SOUL, TOOLS, IDENTITY, USER, HEARTBEAT, MEMORY) are auto-injected. 
No need to run `read` on boot anymore. Use `memory_search` for historical lookup.